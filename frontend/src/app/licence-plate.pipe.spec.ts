import { LicencePlatePipe } from './licence-plate.pipe';

describe('LicencePlatePipe', () => {
  it('create an instance', () => {
    const  pipe = new LicencePlatePipe();
    expect(pipe).toBeTruthy();
  });
});
